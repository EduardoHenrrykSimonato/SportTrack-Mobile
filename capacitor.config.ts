import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.eduardohenrryk.sporttrack',
  appName: 'SportTrack Mobile',
  webDir: 'www'
};

export default config;
